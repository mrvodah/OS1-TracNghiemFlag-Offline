<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vietvan.tracnghiemflag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_menu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrangChuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HuongDanActivity" />
        <activity android:name=".DiemCaoActivity" />
        <activity android:name=".ChonCheDoActivity" />
        <activity android:name=".ManHinh1Activity" />
        <activity android:name=".ManHinh2Activity" />
    </application>

</manifest>